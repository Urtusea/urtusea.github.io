---
import Base from "~/layouts/Base.astro";

const item_files = await Astro.glob(`../../public/imgs/items/*`)
const painting_files = await Astro.glob(`../../public/imgs/paintings/*`)

const items = item_files.map(file => {
    return file.default.split('/').pop().split('.')[0]
});

const paintings = painting_files.map(file => {
    return file.default.split('/').pop().split('.')[0]
});
---

<Base>
<div class="flex flex-col justify-center w-full sm:w-[90%] md:w-[80%] lg:w-[70%] mx-auto gap-5 py-5 px-2 markdown">
    <h3 class=" underline text-2xl sm:text-4xl text-white">Urtusea's Blogs</h3>
    <p class="indent-5 text-white">
        如你所见，这里是本人的 博客 & 文档 汇总，希望我的文章能够对您有所帮助！
    </p>
    <li class="indent-5 test-white">
        E-mail：Urtusea@gmail.com
    </li>
    <li class="indent-5 test-white">
        Codeforces & Atcoder ID：Urtusea
    </li>
    <!-- <a href="/blogs" class="self-center py-2 px-5 my-5 button text-shadow-lg text-2xl">Blogs</a>
    <a href="/templates" class="self-center py-2 px-5 my-5 button text-shadow-lg text-2xl">Templates</a> -->
</div>
</Base>
